# 🚀 testops-copilot

## 📦 1. Установите зависимости
Выполните в терминале: `npm install` или `bun install`

## 🔧 2. Настройте окружение
Создайте файл `.env` в корне проекта и добавьте:  
`VITE_API_URL=http://localhost:8080/api/v1/generate`

## ▶️ 3. Запустите приложение
Выполните: `npm run dev` или `bun run dev`

## 🌐 4. Откройте в браузере
Перейдите по адресу: `http://localhost:5173`

## 💡 Примечание
Убедитесь, что бэкенд-сервер запущен на `http://localhost:8080`